# survey-platform
A full-stack survey management platform that enables administrators to create, manage, and publish dynamic surveys without modifying application code. The system supports multiple question types, including text, email, single and multiple choice, and file uploads, allowing surveys to be configured entirely through the application.
